/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { MUZICA_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const STEM_MINT_DISCRIMINATOR = new Uint8Array([
  200, 99, 1, 221, 243, 249, 7, 7,
]);

export function getStemMintDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(STEM_MINT_DISCRIMINATOR);
}

export type StemMintInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTrack extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTrack extends string
        ? WritableAccount<TAccountTrack>
        : TAccountTrack,
      ...TRemainingAccounts,
    ]
  >;

export type StemMintInstructionData = {
  discriminator: ReadonlyUint8Array;
  trackId: bigint;
  stemMint: Address;
};

export type StemMintInstructionDataArgs = {
  trackId: number | bigint;
  stemMint: Address;
};

export function getStemMintInstructionDataEncoder(): FixedSizeEncoder<StemMintInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['trackId', getU64Encoder()],
      ['stemMint', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: STEM_MINT_DISCRIMINATOR })
  );
}

export function getStemMintInstructionDataDecoder(): FixedSizeDecoder<StemMintInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['trackId', getU64Decoder()],
    ['stemMint', getAddressDecoder()],
  ]);
}

export function getStemMintInstructionDataCodec(): FixedSizeCodec<
  StemMintInstructionDataArgs,
  StemMintInstructionData
> {
  return combineCodec(
    getStemMintInstructionDataEncoder(),
    getStemMintInstructionDataDecoder()
  );
}

export type StemMintAsyncInput<
  TAccountAuthority extends string = string,
  TAccountTrack extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  track?: Address<TAccountTrack>;
  trackId: StemMintInstructionDataArgs['trackId'];
  stemMint: StemMintInstructionDataArgs['stemMint'];
};

export async function getStemMintInstructionAsync<
  TAccountAuthority extends string,
  TAccountTrack extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: StemMintAsyncInput<TAccountAuthority, TAccountTrack>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  StemMintInstruction<TProgramAddress, TAccountAuthority, TAccountTrack>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    track: { value: input.track ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.track.value) {
    accounts.track.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 99, 107])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
        getU64Encoder().encode(expectSome(args.trackId)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.track),
    ],
    data: getStemMintInstructionDataEncoder().encode(
      args as StemMintInstructionDataArgs
    ),
    programAddress,
  } as StemMintInstruction<TProgramAddress, TAccountAuthority, TAccountTrack>);
}

export type StemMintInput<
  TAccountAuthority extends string = string,
  TAccountTrack extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  track: Address<TAccountTrack>;
  trackId: StemMintInstructionDataArgs['trackId'];
  stemMint: StemMintInstructionDataArgs['stemMint'];
};

export function getStemMintInstruction<
  TAccountAuthority extends string,
  TAccountTrack extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: StemMintInput<TAccountAuthority, TAccountTrack>,
  config?: { programAddress?: TProgramAddress }
): StemMintInstruction<TProgramAddress, TAccountAuthority, TAccountTrack> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    track: { value: input.track ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.track),
    ],
    data: getStemMintInstructionDataEncoder().encode(
      args as StemMintInstructionDataArgs
    ),
    programAddress,
  } as StemMintInstruction<TProgramAddress, TAccountAuthority, TAccountTrack>);
}

export type ParsedStemMintInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    track: TAccountMetas[1];
  };
  data: StemMintInstructionData;
};

export function parseStemMintInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedStemMintInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), track: getNextAccount() },
    data: getStemMintInstructionDataDecoder().decode(instruction.data),
  };
}
