/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { MUZICA_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const UPDATE_SHARES_DISCRIMINATOR = new Uint8Array([
  31, 59, 15, 141, 227, 50, 179, 253,
]);

export function getUpdateSharesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_SHARES_DISCRIMINATOR
  );
}

export type UpdateSharesInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTrack extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTrack extends string
        ? WritableAccount<TAccountTrack>
        : TAccountTrack,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateSharesInstructionData = {
  discriminator: ReadonlyUint8Array;
  trackId: bigint;
  newSharesBps: Array<number>;
  contributors: Array<Address>;
};

export type UpdateSharesInstructionDataArgs = {
  trackId: number | bigint;
  newSharesBps: Array<number>;
  contributors: Array<Address>;
};

export function getUpdateSharesInstructionDataEncoder(): Encoder<UpdateSharesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['trackId', getU64Encoder()],
      ['newSharesBps', getArrayEncoder(getU16Encoder())],
      ['contributors', getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_SHARES_DISCRIMINATOR })
  );
}

export function getUpdateSharesInstructionDataDecoder(): Decoder<UpdateSharesInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['trackId', getU64Decoder()],
    ['newSharesBps', getArrayDecoder(getU16Decoder())],
    ['contributors', getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getUpdateSharesInstructionDataCodec(): Codec<
  UpdateSharesInstructionDataArgs,
  UpdateSharesInstructionData
> {
  return combineCodec(
    getUpdateSharesInstructionDataEncoder(),
    getUpdateSharesInstructionDataDecoder()
  );
}

export type UpdateSharesAsyncInput<
  TAccountAuthority extends string = string,
  TAccountTrack extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  track?: Address<TAccountTrack>;
  trackId: UpdateSharesInstructionDataArgs['trackId'];
  newSharesBps: UpdateSharesInstructionDataArgs['newSharesBps'];
  contributors: UpdateSharesInstructionDataArgs['contributors'];
};

export async function getUpdateSharesInstructionAsync<
  TAccountAuthority extends string,
  TAccountTrack extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: UpdateSharesAsyncInput<TAccountAuthority, TAccountTrack>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateSharesInstruction<TProgramAddress, TAccountAuthority, TAccountTrack>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    track: { value: input.track ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.track.value) {
    accounts.track.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 99, 107])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
        getU64Encoder().encode(expectSome(args.trackId)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.track),
    ],
    data: getUpdateSharesInstructionDataEncoder().encode(
      args as UpdateSharesInstructionDataArgs
    ),
    programAddress,
  } as UpdateSharesInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTrack
  >);
}

export type UpdateSharesInput<
  TAccountAuthority extends string = string,
  TAccountTrack extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  track: Address<TAccountTrack>;
  trackId: UpdateSharesInstructionDataArgs['trackId'];
  newSharesBps: UpdateSharesInstructionDataArgs['newSharesBps'];
  contributors: UpdateSharesInstructionDataArgs['contributors'];
};

export function getUpdateSharesInstruction<
  TAccountAuthority extends string,
  TAccountTrack extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: UpdateSharesInput<TAccountAuthority, TAccountTrack>,
  config?: { programAddress?: TProgramAddress }
): UpdateSharesInstruction<TProgramAddress, TAccountAuthority, TAccountTrack> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    track: { value: input.track ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.track),
    ],
    data: getUpdateSharesInstructionDataEncoder().encode(
      args as UpdateSharesInstructionDataArgs
    ),
    programAddress,
  } as UpdateSharesInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTrack
  >);
}

export type ParsedUpdateSharesInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    track: TAccountMetas[1];
  };
  data: UpdateSharesInstructionData;
};

export function parseUpdateSharesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateSharesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), track: getNextAccount() },
    data: getUpdateSharesInstructionDataDecoder().decode(instruction.data),
  };
}
