/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { MUZICA_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const ESCROW_DEPOSIT_DISCRIMINATOR = new Uint8Array([
  137, 100, 252, 219, 140, 205, 146, 215,
]);

export function getEscrowDepositDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ESCROW_DEPOSIT_DISCRIMINATOR
  );
}

export type EscrowDepositInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountTrack extends string | AccountMeta<string> = string,
  TAccountEscrowTokenAccount extends string | AccountMeta<string> = string,
  TAccountPayerTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountTrack extends string
        ? WritableAccount<TAccountTrack>
        : TAccountTrack,
      TAccountEscrowTokenAccount extends string
        ? WritableAccount<TAccountEscrowTokenAccount>
        : TAccountEscrowTokenAccount,
      TAccountPayerTokenAccount extends string
        ? WritableAccount<TAccountPayerTokenAccount>
        : TAccountPayerTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EscrowDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  trackId: bigint;
  authority: Address;
};

export type EscrowDepositInstructionDataArgs = {
  amount: number | bigint;
  trackId: number | bigint;
  authority: Address;
};

export function getEscrowDepositInstructionDataEncoder(): FixedSizeEncoder<EscrowDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
      ['trackId', getU64Encoder()],
      ['authority', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ESCROW_DEPOSIT_DISCRIMINATOR })
  );
}

export function getEscrowDepositInstructionDataDecoder(): FixedSizeDecoder<EscrowDepositInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
    ['trackId', getU64Decoder()],
    ['authority', getAddressDecoder()],
  ]);
}

export function getEscrowDepositInstructionDataCodec(): FixedSizeCodec<
  EscrowDepositInstructionDataArgs,
  EscrowDepositInstructionData
> {
  return combineCodec(
    getEscrowDepositInstructionDataEncoder(),
    getEscrowDepositInstructionDataDecoder()
  );
}

export type EscrowDepositAsyncInput<
  TAccountPayer extends string = string,
  TAccountTrack extends string = string,
  TAccountEscrowTokenAccount extends string = string,
  TAccountPayerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  track?: Address<TAccountTrack>;
  escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
  payerTokenAccount: Address<TAccountPayerTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: EscrowDepositInstructionDataArgs['amount'];
  trackId: EscrowDepositInstructionDataArgs['trackId'];
  authority: EscrowDepositInstructionDataArgs['authority'];
};

export async function getEscrowDepositInstructionAsync<
  TAccountPayer extends string,
  TAccountTrack extends string,
  TAccountEscrowTokenAccount extends string,
  TAccountPayerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: EscrowDepositAsyncInput<
    TAccountPayer,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountPayerTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  EscrowDepositInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountPayerTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: false },
    track: { value: input.track ?? null, isWritable: true },
    escrowTokenAccount: {
      value: input.escrowTokenAccount ?? null,
      isWritable: true,
    },
    payerTokenAccount: {
      value: input.payerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.track.value) {
    accounts.track.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 99, 107])),
        getAddressEncoder().encode(expectSome(args.authority)),
        getU64Encoder().encode(expectSome(args.trackId)),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.track),
      getAccountMeta(accounts.escrowTokenAccount),
      getAccountMeta(accounts.payerTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getEscrowDepositInstructionDataEncoder().encode(
      args as EscrowDepositInstructionDataArgs
    ),
    programAddress,
  } as EscrowDepositInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountPayerTokenAccount,
    TAccountTokenProgram
  >);
}

export type EscrowDepositInput<
  TAccountPayer extends string = string,
  TAccountTrack extends string = string,
  TAccountEscrowTokenAccount extends string = string,
  TAccountPayerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  track: Address<TAccountTrack>;
  escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
  payerTokenAccount: Address<TAccountPayerTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: EscrowDepositInstructionDataArgs['amount'];
  trackId: EscrowDepositInstructionDataArgs['trackId'];
  authority: EscrowDepositInstructionDataArgs['authority'];
};

export function getEscrowDepositInstruction<
  TAccountPayer extends string,
  TAccountTrack extends string,
  TAccountEscrowTokenAccount extends string,
  TAccountPayerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: EscrowDepositInput<
    TAccountPayer,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountPayerTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): EscrowDepositInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountTrack,
  TAccountEscrowTokenAccount,
  TAccountPayerTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: false },
    track: { value: input.track ?? null, isWritable: true },
    escrowTokenAccount: {
      value: input.escrowTokenAccount ?? null,
      isWritable: true,
    },
    payerTokenAccount: {
      value: input.payerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.track),
      getAccountMeta(accounts.escrowTokenAccount),
      getAccountMeta(accounts.payerTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getEscrowDepositInstructionDataEncoder().encode(
      args as EscrowDepositInstructionDataArgs
    ),
    programAddress,
  } as EscrowDepositInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountPayerTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedEscrowDepositInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    track: TAccountMetas[1];
    escrowTokenAccount: TAccountMetas[2];
    payerTokenAccount: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: EscrowDepositInstructionData;
};

export function parseEscrowDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedEscrowDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      track: getNextAccount(),
      escrowTokenAccount: getNextAccount(),
      payerTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getEscrowDepositInstructionDataDecoder().decode(instruction.data),
  };
}
