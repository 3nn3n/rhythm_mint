/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { MUZICA_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const INITIALIZE_TRACK_DISCRIMINATOR = new Uint8Array([
  14, 160, 200, 167, 109, 13, 15, 196,
]);

export function getInitializeTrackDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_TRACK_DISCRIMINATOR
  );
}

export type InitializeTrackInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTrack extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTrack extends string
        ? WritableAccount<TAccountTrack>
        : TAccountTrack,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeTrackInstructionData = {
  discriminator: ReadonlyUint8Array;
  trackId: bigint;
  title: string;
  cid: string;
  masterHash: ReadonlyUint8Array;
  contributors: Array<Address>;
  sharesBps: Array<number>;
};

export type InitializeTrackInstructionDataArgs = {
  trackId: number | bigint;
  title: string;
  cid: string;
  masterHash: ReadonlyUint8Array;
  contributors: Array<Address>;
  sharesBps: Array<number>;
};

export function getInitializeTrackInstructionDataEncoder(): Encoder<InitializeTrackInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['trackId', getU64Encoder()],
      ['title', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['cid', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['masterHash', fixEncoderSize(getBytesEncoder(), 32)],
      ['contributors', getArrayEncoder(getAddressEncoder())],
      ['sharesBps', getArrayEncoder(getU16Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_TRACK_DISCRIMINATOR })
  );
}

export function getInitializeTrackInstructionDataDecoder(): Decoder<InitializeTrackInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['trackId', getU64Decoder()],
    ['title', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['cid', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['masterHash', fixDecoderSize(getBytesDecoder(), 32)],
    ['contributors', getArrayDecoder(getAddressDecoder())],
    ['sharesBps', getArrayDecoder(getU16Decoder())],
  ]);
}

export function getInitializeTrackInstructionDataCodec(): Codec<
  InitializeTrackInstructionDataArgs,
  InitializeTrackInstructionData
> {
  return combineCodec(
    getInitializeTrackInstructionDataEncoder(),
    getInitializeTrackInstructionDataDecoder()
  );
}

export type InitializeTrackAsyncInput<
  TAccountAuthority extends string = string,
  TAccountTrack extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  track?: Address<TAccountTrack>;
  systemProgram?: Address<TAccountSystemProgram>;
  trackId: InitializeTrackInstructionDataArgs['trackId'];
  title: InitializeTrackInstructionDataArgs['title'];
  cid: InitializeTrackInstructionDataArgs['cid'];
  masterHash: InitializeTrackInstructionDataArgs['masterHash'];
  contributors: InitializeTrackInstructionDataArgs['contributors'];
  sharesBps: InitializeTrackInstructionDataArgs['sharesBps'];
};

export async function getInitializeTrackInstructionAsync<
  TAccountAuthority extends string,
  TAccountTrack extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: InitializeTrackAsyncInput<
    TAccountAuthority,
    TAccountTrack,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeTrackInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTrack,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    track: { value: input.track ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.track.value) {
    accounts.track.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 99, 107])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
        getU64Encoder().encode(expectSome(args.trackId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.track),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeTrackInstructionDataEncoder().encode(
      args as InitializeTrackInstructionDataArgs
    ),
    programAddress,
  } as InitializeTrackInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTrack,
    TAccountSystemProgram
  >);
}

export type InitializeTrackInput<
  TAccountAuthority extends string = string,
  TAccountTrack extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  track: Address<TAccountTrack>;
  systemProgram?: Address<TAccountSystemProgram>;
  trackId: InitializeTrackInstructionDataArgs['trackId'];
  title: InitializeTrackInstructionDataArgs['title'];
  cid: InitializeTrackInstructionDataArgs['cid'];
  masterHash: InitializeTrackInstructionDataArgs['masterHash'];
  contributors: InitializeTrackInstructionDataArgs['contributors'];
  sharesBps: InitializeTrackInstructionDataArgs['sharesBps'];
};

export function getInitializeTrackInstruction<
  TAccountAuthority extends string,
  TAccountTrack extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: InitializeTrackInput<
    TAccountAuthority,
    TAccountTrack,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeTrackInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountTrack,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    track: { value: input.track ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.track),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeTrackInstructionDataEncoder().encode(
      args as InitializeTrackInstructionDataArgs
    ),
    programAddress,
  } as InitializeTrackInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountTrack,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeTrackInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    track: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeTrackInstructionData;
};

export function parseInitializeTrackInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeTrackInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      track: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeTrackInstructionDataDecoder().decode(instruction.data),
  };
}
