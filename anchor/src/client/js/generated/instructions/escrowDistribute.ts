/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { MUZICA_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const ESCROW_DISTRIBUTE_DISCRIMINATOR = new Uint8Array([
  130, 202, 39, 127, 212, 7, 91, 213,
]);

export function getEscrowDistributeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ESCROW_DISTRIBUTE_DISCRIMINATOR
  );
}

export type EscrowDistributeInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountTrack extends string | AccountMeta<string> = string,
  TAccountEscrowTokenAccount extends string | AccountMeta<string> = string,
  TAccountTrackAuthority extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrack extends string
        ? WritableAccount<TAccountTrack>
        : TAccountTrack,
      TAccountEscrowTokenAccount extends string
        ? WritableAccount<TAccountEscrowTokenAccount>
        : TAccountEscrowTokenAccount,
      TAccountTrackAuthority extends string
        ? ReadonlyAccount<TAccountTrackAuthority>
        : TAccountTrackAuthority,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EscrowDistributeInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  trackId: bigint;
};

export type EscrowDistributeInstructionDataArgs = {
  amount: number | bigint;
  trackId: number | bigint;
};

export function getEscrowDistributeInstructionDataEncoder(): FixedSizeEncoder<EscrowDistributeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
      ['trackId', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ESCROW_DISTRIBUTE_DISCRIMINATOR })
  );
}

export function getEscrowDistributeInstructionDataDecoder(): FixedSizeDecoder<EscrowDistributeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
    ['trackId', getU64Decoder()],
  ]);
}

export function getEscrowDistributeInstructionDataCodec(): FixedSizeCodec<
  EscrowDistributeInstructionDataArgs,
  EscrowDistributeInstructionData
> {
  return combineCodec(
    getEscrowDistributeInstructionDataEncoder(),
    getEscrowDistributeInstructionDataDecoder()
  );
}

export type EscrowDistributeAsyncInput<
  TAccountTrack extends string = string,
  TAccountEscrowTokenAccount extends string = string,
  TAccountTrackAuthority extends string = string,
  TAccountAuthority extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  track?: Address<TAccountTrack>;
  escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
  trackAuthority?: Address<TAccountTrackAuthority>;
  authority: TransactionSigner<TAccountAuthority>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: EscrowDistributeInstructionDataArgs['amount'];
  trackId: EscrowDistributeInstructionDataArgs['trackId'];
};

export async function getEscrowDistributeInstructionAsync<
  TAccountTrack extends string,
  TAccountEscrowTokenAccount extends string,
  TAccountTrackAuthority extends string,
  TAccountAuthority extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: EscrowDistributeAsyncInput<
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountTrackAuthority,
    TAccountAuthority,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  EscrowDistributeInstruction<
    TProgramAddress,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountTrackAuthority,
    TAccountAuthority,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    track: { value: input.track ?? null, isWritable: true },
    escrowTokenAccount: {
      value: input.escrowTokenAccount ?? null,
      isWritable: true,
    },
    trackAuthority: { value: input.trackAuthority ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.track.value) {
    accounts.track.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 99, 107])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
        getU64Encoder().encode(expectSome(args.trackId)),
      ],
    });
  }
  if (!accounts.trackAuthority.value) {
    accounts.trackAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 99, 107])),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
        getU64Encoder().encode(expectSome(args.trackId)),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.track),
      getAccountMeta(accounts.escrowTokenAccount),
      getAccountMeta(accounts.trackAuthority),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getEscrowDistributeInstructionDataEncoder().encode(
      args as EscrowDistributeInstructionDataArgs
    ),
    programAddress,
  } as EscrowDistributeInstruction<
    TProgramAddress,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountTrackAuthority,
    TAccountAuthority,
    TAccountTokenProgram
  >);
}

export type EscrowDistributeInput<
  TAccountTrack extends string = string,
  TAccountEscrowTokenAccount extends string = string,
  TAccountTrackAuthority extends string = string,
  TAccountAuthority extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  track: Address<TAccountTrack>;
  escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
  trackAuthority: Address<TAccountTrackAuthority>;
  authority: TransactionSigner<TAccountAuthority>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: EscrowDistributeInstructionDataArgs['amount'];
  trackId: EscrowDistributeInstructionDataArgs['trackId'];
};

export function getEscrowDistributeInstruction<
  TAccountTrack extends string,
  TAccountEscrowTokenAccount extends string,
  TAccountTrackAuthority extends string,
  TAccountAuthority extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MUZICA_PROGRAM_ADDRESS,
>(
  input: EscrowDistributeInput<
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountTrackAuthority,
    TAccountAuthority,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): EscrowDistributeInstruction<
  TProgramAddress,
  TAccountTrack,
  TAccountEscrowTokenAccount,
  TAccountTrackAuthority,
  TAccountAuthority,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MUZICA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    track: { value: input.track ?? null, isWritable: true },
    escrowTokenAccount: {
      value: input.escrowTokenAccount ?? null,
      isWritable: true,
    },
    trackAuthority: { value: input.trackAuthority ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.track),
      getAccountMeta(accounts.escrowTokenAccount),
      getAccountMeta(accounts.trackAuthority),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getEscrowDistributeInstructionDataEncoder().encode(
      args as EscrowDistributeInstructionDataArgs
    ),
    programAddress,
  } as EscrowDistributeInstruction<
    TProgramAddress,
    TAccountTrack,
    TAccountEscrowTokenAccount,
    TAccountTrackAuthority,
    TAccountAuthority,
    TAccountTokenProgram
  >);
}

export type ParsedEscrowDistributeInstruction<
  TProgram extends string = typeof MUZICA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    track: TAccountMetas[0];
    escrowTokenAccount: TAccountMetas[1];
    trackAuthority: TAccountMetas[2];
    authority: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: EscrowDistributeInstructionData;
};

export function parseEscrowDistributeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedEscrowDistributeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      track: getNextAccount(),
      escrowTokenAccount: getNextAccount(),
      trackAuthority: getNextAccount(),
      authority: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getEscrowDistributeInstructionDataDecoder().decode(instruction.data),
  };
}
