/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from 'gill';

export const TRACK_DISCRIMINATOR = new Uint8Array([
  148, 45, 9, 235, 14, 15, 36, 159,
]);

export function getTrackDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TRACK_DISCRIMINATOR);
}

export type Track = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  trackId: bigint;
  title: string;
  cid: string;
  masterHash: ReadonlyUint8Array;
  contributors: Array<Address>;
  shares: Array<number>;
  stemMints: Array<Address>;
  royaltyVersion: number;
  bump: number;
};

export type TrackArgs = {
  authority: Address;
  trackId: number | bigint;
  title: string;
  cid: string;
  masterHash: ReadonlyUint8Array;
  contributors: Array<Address>;
  shares: Array<number>;
  stemMints: Array<Address>;
  royaltyVersion: number;
  bump: number;
};

/** Gets the encoder for {@link TrackArgs} account data. */
export function getTrackEncoder(): Encoder<TrackArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['authority', getAddressEncoder()],
      ['trackId', getU64Encoder()],
      ['title', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['cid', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['masterHash', fixEncoderSize(getBytesEncoder(), 32)],
      ['contributors', getArrayEncoder(getAddressEncoder())],
      ['shares', getArrayEncoder(getU16Encoder())],
      ['stemMints', getArrayEncoder(getAddressEncoder())],
      ['royaltyVersion', getU32Encoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TRACK_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link Track} account data. */
export function getTrackDecoder(): Decoder<Track> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['authority', getAddressDecoder()],
    ['trackId', getU64Decoder()],
    ['title', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['cid', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['masterHash', fixDecoderSize(getBytesDecoder(), 32)],
    ['contributors', getArrayDecoder(getAddressDecoder())],
    ['shares', getArrayDecoder(getU16Decoder())],
    ['stemMints', getArrayDecoder(getAddressDecoder())],
    ['royaltyVersion', getU32Decoder()],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Track} account data. */
export function getTrackCodec(): Codec<TrackArgs, Track> {
  return combineCodec(getTrackEncoder(), getTrackDecoder());
}

export function decodeTrack<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Track, TAddress>;
export function decodeTrack<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Track, TAddress>;
export function decodeTrack<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Track, TAddress> | MaybeAccount<Track, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTrackDecoder()
  );
}

export async function fetchTrack<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Track, TAddress>> {
  const maybeAccount = await fetchMaybeTrack(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTrack<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Track, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTrack(maybeAccount);
}

export async function fetchAllTrack(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Track>[]> {
  const maybeAccounts = await fetchAllMaybeTrack(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTrack(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Track>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTrack(maybeAccount));
}
